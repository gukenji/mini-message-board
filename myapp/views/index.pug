extends layout

block content
  h1= title
  p Welcome to #{title}
  <br>
  a(href='/new') Enviar mensagem
  <br>
  ul
    each message in messages 
      div(class='div-class')
      h3= `Nome: ${message.user}`
      h4= `Mensagem: ${message.text}`
      <br>
      
